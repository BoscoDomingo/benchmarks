# Array.includes() vs Set.has()

Compares the performance of **Array.includes()** versus **Set.has()** for checking if a value exists in a collection.

Note: obviously the Set will be far faster, this is just to illustrate in real-world applications just how much faster.
